# Dependencies
node_modules/
netlify/functions/node_modules/

# Secrets / env
.env
*.env
netlify/functions/.env

# Build output (regenerated by build.js on every deploy)
_redirects
_headers
robots.txt
sitemap.xml

# Local scratch / working files
scratchpad-*
scratchpad/

# Uncompressed source images kept locally as a backup, not for deploy
images/_originals_uncompressed/

# OS / editor cruft
.DS_Store
*.swp
.vscode/
.idea/

# Claude Code per-user local settings (not shared)
.claude/settings.local.json
